const Navbar = (props) => {
    return (
        <nav className="navbar w-100 bg-dark bg-gradient position-fixed top-0 py-2">
            <div className="container-fluid px-3">
                <span className="navbar-brand text-light">Chat Room</span>
            </div>
        </nav>
    );
};

export default Navbar;
